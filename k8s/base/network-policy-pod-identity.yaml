# Security: Network Policy for Pod Identity micro-segmentation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hello-world-pod-identity-network-policy
  namespace: hello-world
spec:
  podSelector:
    matchLabels:
      app: hello-world-pod-identity
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from ingress controller
  - from:
    - namespaceSelector:
        matchLabels:
          name: kube-system
    ports:
    - protocol: TCP
      port: 3000
  # Allow ingress from same namespace pods
  - from:
    - podSelector:
        matchLabels:
          app: hello-world
    ports:
    - protocol: TCP
      port: 3000
  egress:
  # Allow DNS resolution
  - to:
    - namespaceSelector:
        matchLabels:
          name: kube-system
    ports:
    - protocol: UDP
      port: 53
  # Allow HTTPS outbound for AWS services
  - to: []
    ports:
    - protocol: TCP
      port: 443
  # Allow HTTP outbound for health checks
  - to: []
    ports:
    - protocol: TCP
      port: 80