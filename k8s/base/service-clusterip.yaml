# Alternative: ClusterIP service (completely free)
apiVersion: v1
kind: Service
metadata:
  name: hello-world-service-clusterip
  namespace: hello-world
spec:
  selector:
    app: hello-world
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP  # Only accessible within cluster
